<h3>Editing User Information</h3>

<%= render 'form', enable_type_select: false %>

<%= link_to 'Show', user_path(@user), class: 'btn btn-small' %>
<%= link_to 'Delete', user_path(@user), method: :delete, data: {confirm: 'Are you sure you want to permanently delete your account?'}, class: 'btn btn-small' %>
